apiVersion: 1
datasources:
  - name: MySQL
    type: mysql
    url: mysql:3306
    database: shortsdb
    user: shorts
    secureJsonData:
      password: ${MYSQL_PASSWORD}
    jsonData:
      maxOpenConns: 10
      maxIdleConns: 10
      connMaxLifetime: 14400
